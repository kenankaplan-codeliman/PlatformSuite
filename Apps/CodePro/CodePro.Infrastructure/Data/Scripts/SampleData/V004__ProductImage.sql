-- =============================================
-- CodePro SampleData / V004 — ProductImage
--
-- ProductImage tablosu BYTEA (binary) alanlar içerir (image_bytes, thumbnail_bytes).
-- Anlamlı bir sample data için gerçek görüntü içeriği gerekir; sample data ortamında
-- placeholder oluşturmak pratik olmadığından bu dosya bilinçli olarak boş bırakılmıştır.
-- Görüntüler ürün detay sayfasından UI üzerinden yüklenmelidir.
--
-- Migrator script'i embed eder ve çalıştırır; içerik no-op olduğundan etki etmez.
-- =============================================

-- intentionally empty
SELECT 1 WHERE FALSE;
